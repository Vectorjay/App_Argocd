apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Vectorjay/App_Argocd.git  # 🔹 Your Git repo
    targetRevision: main                      # 🔹 Branch to track
    path: manifests                           # 🔹 Path to Kubernetes YAML files
  destination:
    server: https://kubernetes.default.svc
    namespace: my-app
  syncPolicy:
    automated:
      prune: true    # 🔹 Delete resources not in Git
      selfHeal: true # 🔹 Fix drift if manual changes occur
    syncOptions:
      - CreateNamespace=true # 🔹 Ensure namespace is created

